import { ListaProveedorPipe } from "./lista-proveedor.pipe";


describe('ListaProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new ListaProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});