import { ListarEmpleadosPipe } from './listar-empleados.pipe';

describe('ListarEmpleadosPipe', () => {
  it('create an instance', () => {
    const pipe = new ListarEmpleadosPipe();
    expect(pipe).toBeTruthy();
  });
});
