import { ListarMembresiaPipe } from "./lista-proveedor.pipe";

describe('ListarMembresiaPipe', () => {
  it('create an instance', () => {
    const pipe = new ListarMembresiaPipe();
    expect(pipe).toBeTruthy();
  });
});